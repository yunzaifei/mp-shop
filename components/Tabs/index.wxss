/* components/Tabs/index.wxss */
.tabs-title {
  display: flex;
}
.tabs-item {
  flex: 1;
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 15rpx 0;
}
.active {
  color: var(--themeColor);
  border-bottom: 5rpx solid;
}